# Done today — 2026-08-10

## markibx/mawtarx audit — measured the catalog link's false precision, filed 3 tiered issues

- Measured against the real 8,315-row corpus that the `catalog_car_id` ladder reports `exact` for 5,078 of 5,721 matches (88.8%) that land on catch-all `gen1`/QID shells, 50.8% of them spanning ≥10 years (`hyundai:accent:gen1` = 2006–2026, `toyota:landcruiser:j70` = 42y), so a 2006 and a 2024 Accent share one spec sheet and one MSRP baseline — the linker itself is sound at 91.8% on resolvable rows.
- Reframed spec depth from a 1,623-generation problem to a ~120-generation one: all 5,721 exact matches land on 260 generations, 103 cover 80% of demand, and 122 of the 260 carry no spec (43.6% of real listings could show a spec sheet).
- Filed [delawer33/xwresearch#36](https://github.com/delawer33/xwresearch/issues/36) (verification holes, open auth gates, unguarded writes, misleading docs), [#37](https://github.com/delawer33/xwresearch/issues/37) (false precision, overstated product knowledge), [#38](https://github.com/delawer33/xwresearch/issues/38) (zero-dep doctrine conflict, forked platform types, inverted xwapi ownership) — problems only, no remedies.
- Proved pre-existing breakage so the next agent doesn't re-debug it: 16 failed / 2,843 passed / 20 skipped across both families, with markibx `main` committed-red at 8 failures on a clean tree, and `Taskfile.yml:89-91` scoring zero-collected-tests as a pass (which is why `markibx-connect-api`'s 1,037 LOC including `auth.py` reports clean); also established authoritative connector counts by importing the registry — 245 registered / 137 ACTIVE / 14 prod-swept, against 671/86/~7 in the docs.
- Deliberately changed no code and closed no issues; corrected three of my own audits' claims that failed verification (spine browse is not broken — only `year_source` is None; the two connect repos do not duplicate each other; there are exactly 14 declared-synthetic sources).

**Left open**

- All of #36/#37/#38 — recommended Tier 1 sequencing is: prune worktrees, then the Taskfile exit-code fix alone (nothing else is verifiable until it lands), then auth + store-integrity + docs in parallel, then the stale tests last.
- Whether `MAWTARX_JWT_SECRET`/`MARKIBX_JWT_SECRET`/`MAWTARX_ADMIN_TOKEN` are actually set on the dev box is unverified — `/etc/*.env` is root-owned; both APIs default to a hardcoded literal with no boot guard, and only karaa's deploy generates one.
- 7 fully-merged worktrees under `repos/*/.claude/worktrees/` (~40MB, none gitignored) still make every grep return 3 contradictory copies — this corrupted two of my own audit agents' counts; `worktrees/mawtarx-coverage` holds 1 genuinely unmerged commit.
- Another session is implementing #36's atomic-write item right now (new `markibx/src/exonware/markibx/atomic_io.py` + the six write sites); it touches `spine_seed_writer.py`, which is also where #37's `origin`-drop fix lands — conflict to coordinate.
- `markibx-web` still holds 9 unpushed commits of finished spine-console work behind a 403 push wall.

## Kuwait activation, session 1 of 2 — coverage KPI, KW probe, connector repairs, prod sweep table

- **mawtarx `0fe9e74..195954c` (pushed)** — `coverage.py` + `mawtarx coverage` compute per-country K1/K2/K3 off the *real* pricing engine (K1 by re-running the orchestrator with foreign rows withheld, since `similar_match` spans five tiers and can't be classified on method alone); measured Saudi at **K1 68.5% / K2 96.0%**, both past the ≥60/≥80 bars, on 7,995 rows. Suite 590 → 604.
- **mawtarx-connect `15b88d9..57f8d65` (pushed)** — the five live Kuwaiti sources enter `sweep_profiles._DEFAULTS` (14 → 19) with every `full` window set from *measured* pagination exhaustion and paired with a shallow incremental; `q84sale.kw`, `dubizzle.kw`, `waseet.kw`, `yallamotor.kw` provably absent. Suite 522 pass / 1 skip.
- **Three real connector bugs, two on already-live countries** — `opensooq` and `hatla2ee` cover photos were 0% because the adapters never read the payload's image field (now 100%, HTTP-verified on 24 URLs, reaching SA+AE in prod now and KW on deploy), and `motorgy`'s price regex required digits immediately after `>` while the site also serves `> 14,650 KWD`, which silently dropped **every row of the source**.
- **`dubizzle` has been sweeping zero rows in production** — it sits in `_DEFAULTS`, fires daily, and serves a JS fingerprint interstitial at HTTP 200 on `.sa`, `.kw` and `.bh`. Not parser-fixable, not bypassable; needs a human decision (drop it, or find a sanctioned feed).
- **Deliberately not built:** `city` defaults for `alsayer_toyota.kw`/`motorgy.kw` (neither site publishes a location — a constant would be invented data), the `yousale.kw` adapter (new-connector work, #14), and a `dubizzle` workaround. **Kuwait's realistic ceiling is thin:** 4Sale (~13.5k ads) is 403 behind CloudFront and dubizzle is walled, leaving ~6k rows from essentially opensooq + motorgy.
- **Blocked, not done:** the VPS deploy — `scp` to the box is classifier-refused, so mawtarx-connect is pushed but *not* shipped; steps and verification are copy-paste ready in `mawtarx-connect/docs/kw-deploy-handoff.md`, and `/opt/mawtarx-connect-api/.venv` still lacks `xworjson`. The UAE K1/K2 baseline is also unmeasured — no AE rows exist locally and `mawtarx.com/api/...` 302s to `/_gate/login`.
- **Mess to clean up:** both `repos/mawtarx` and `repos/mawtarx-connect` main checkouts sit on another session's branch `fix/issue-36-unguarded-writes`, which now wrongly carries my merge commit (`195954c` / `57f8d65`) on top of it. `main` and `origin/main` are correct; that branch needs resetting to `0fe9e74` / `15b88d9`. `git reset` is classifier-refused here. Their uncommitted edits are intact.

## Issue #36 point 5 — unguarded writes closed across 6 repos, merged to main, deploy blocked

- Routed every system-of-record write through one atomic writer per layer: `xwsystem.io.safe_write_text/bytes` in the five repos that depend on xwsystem (deleting all 3 forked `_atomic_write` copies), and a new stdlib `markibx/atomic_io.py` in markibx core, which declares `dependencies = []` and imports no `exonware.xw*` package by design.
- Stopped the corrupt-read → empty-RAM → truncating-write path in both mawtarx file stores and both APIs' durable token providers: an absent file is still a fresh store, a present-but-unreadable one raises `StoreCorruptError`/`TokenStoreCorruptError` with the bytes left on disk for an operator to restore.
- Made mawtarx-api honest about a store it could not open — a lost fence (`XWStorageDbConcurrencyError`) and an unreadable store now propagate instead of falling back to a second writer or to memory, a degraded store is never seeded with the 250 synthetic rows, and `/health` carries `store_degraded`; catalog brand/currency/FX syncs write under `MAWTARX_CATALOG_SNAPSHOT_DIR` instead of the installed package directory, with reads still falling back to the packaged copy.
- Found and fixed two defects the issue did not name: `mawtarx reprice --store <plain .json>` silently re-encoded the file as binary xwjson (the store now keeps the encoding it opened on), and `test_reprice_cli_reprices_a_file_store` was asserting over the resulting empty store — passing vacuously.
- Proved no regression by baselining the same suites from `origin/main` worktrees: failure sets identical on both sides (markibx 8, markibx-api 3, markibx-connect 2, mawtarx-api 3, mawtarx and mawtarx-connect 0), all pre-existing committed-red from point 1; 19 new tests green, `comment_lint` clean on all six.

**Left open**

- **Deploy did not happen** — the VPS deploy lock was held by another live session (`4e64bbe6`, "deploy mawtarx-connect KW sweep table"). All six repos are merged and pushed to `main`; the dev box still runs the pre-change code.
- **`xwsystem.io.safe_write_text` never fsyncs.** Point 5 asked for fsync before `os.replace` on the token store; the estate writer does not do it (markibx's new stdlib one does). The layer-correct fix is in xwsystem and slows every atomic write in the estate — a foundation decision left to the owner.
- **Undeclared dependency, pre-existing and now wider.** `mawtarx` and `markibx-connect` import `exonware.xwsystem` at top level while their `pyproject.toml`s do not declare it (precedent: `sweep_ledger.py`, `run_ledger.py`). Fixing it edits `pyproject.toml`, an ASK-OWNER lease file.
- **Issue #36 stays open** — only point 5 was in scope; points 1, 2, 3, 4, 6 and 7 are untouched, and another session holds `fix/issue-36-auth-gates` worktrees for point 3.
- `task lint:comments -- <repo>` is broken: it passes the bare repo name to a script that wants `repos/<name>`. Run it by path until someone fixes the Taskfile.
