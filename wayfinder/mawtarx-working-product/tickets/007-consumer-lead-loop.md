# 007 — Consumer journey: is reveal-phone enough, or must we close the lead loop?

- Type: `wayfinder:grilling` (HITL)
- Status: parked — deferred to the consumer-marketplace phase (in scope, not dropped)
- Blocked by: consumer phase (post-API, frontend-team availability) — see 001
- Assignee: —

## Question

The marketplace ends the buyer journey at "reveal phone/whatsapp" — there is no on-platform
lead/message-send. For a Saudi "usable end-to-end UX", is off-platform contact acceptable for
launch (matches how most Saudi marketplaces work), or must we build an on-platform lead/enquiry
flow (and the seller-side inbox that implies)? Weigh against the parity benchmark (002).

Resolve to: reveal-phone-only vs on-platform-lead for launch + the minimal loop if the latter.
